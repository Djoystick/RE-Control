const fs = require('fs');
const path = 'C:/Users/Djoystick/.gemini/antigravity/brain/f7445806-9b74-4b13-956c-f99ac7d43d07/re_control_roadmap.md';
let content = fs.readFileSync(path, 'utf8');

content = content.replace(/### 3\.3 — DonationAlerts \/ Boosty интеграция[\s\S]*?### 3\.4/g, 
### 3.3 — DonationAlerts / Boosty интеграция

**Логика (Бэкенд):**
- Подключение к API DonationAlerts / Boosty.
- Донат от зрителя = мгновенный приоритетный эффект без голосования.
- Проброс логики VIP-очереди в движок эффектов (VotingManager).

### 3.4);

content = content.replace(/### 3\.5 — Конфигуратор эффектов \(No-Code\)[\s\S]*?---/g,
### 3.5 — Конфигуратор эффектов и Донатных Тиров

**Полноэкранный UI настройки сессии:**
- **Управление пулом:** Возможность включать/отключать определенные эффекты для конкретной игровой сессии (чтобы избежать перенасыщения).
- **Донатные Тиры (Градация цен):** Стример может вбивать суммы (100₽, 500₽ и т.д.) и мягким перетаскиванием (drag-and-drop) раскидывать эффекты из общего пула по ценовым категориям.
- **Интерфейс:** Открывается на весь экран поверх приложения, имеет кнопки "Сохранить и закрыть" и "Назад" (без сохранения).
- **No-Code создание:** (В будущем) конструктор кастомных эффектов из базовых блоков.

---);

fs.writeFileSync(path, content, 'utf8');
console.log('Fixed roadmap logic');
