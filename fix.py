import re

path = r'C:\Users\Djoystick\.gemini\antigravity\brain\f7445806-9b74-4b13-956c-f99ac7d43d07\re_control_roadmap.md'
with open(path, 'r', encoding='utf-8') as f:
    text = f.read()

# Fix the accidental insertion
text = re.sub(r'Для 15-часовых сюжетных игр постоянный спам \(раз в 30 секунд\) ведет к выгоранию стримера\.\n\n### 3\.3 — DonationAlerts / Boosty интеграция\n\n\*\*Логика \(Бэкенд\):\*\*\n- Подключение к API DonationAlerts / Boosty\.\n- Донат от зрителя = мгновенный приоритетный эффект без голосования\.\n- Проброс логики VIP-очереди в движок эффектов \(VotingManager\)\.\n\n- \*\*Настройка интенсивности в UI:\*\*',
    'Для 15-часовых сюжетных игр постоянный спам (раз в 30 секунд) ведет к выгоранию стримера.\n- **Пул эффектов:** Расширен до 21 эффекта с упором на "мягкий хаос" и психологические трюки (фантомные звуки, моргание камеры), а не только на хардкор (см. effects_design.md).\n- **Настройка интенсивности в UI:**', text)

# Fix 3.3
text = re.sub(r'  \*\*Логика:\*\*\n  - Донат от зрителя = мгновенный приоритетный эффект без голосования\.\n  - \*\*Тиры \(Градация цен\):\*\* Гибкая настройка стоимости\. Чем сумасшедшее эффект, тем он дороже\.\n  - \*\*UI Настройки:\*\* Отдельный красивый экран, где стример может создавать ценовые категории \(например, 100₽, 500₽, 1000₽\) и мягким перетаскиванием \(drag-and-drop\) раскидывать эффекты из общего пула по этим категориям\.',
    '**Логика (Бэкенд):**\n- Подключение к API DonationAlerts / Boosty.\n- Донат от зрителя = мгновенный приоритетный эффект без голосования.\n- Проброс логики VIP-очереди в движок эффектов (VotingManager).', text)

# Fix 3.5
text = re.sub(r'### 3\.5 — Конфигуратор эффектов \(No-Code\)[\s\S]*?---',
    '''### 3.5 — Конфигуратор эффектов и Донатных Тиров

**Полноэкранный UI настройки сессии:**
- **Управление пулом:** Возможность включать/отключать определенные эффекты для конкретной игровой сессии (чтобы избежать перенасыщения).
- **Донатные Тиры (Градация цен):** Гибкая настройка стоимости эффектов. Стример может вбивать суммы (100₽, 500₽ и т.д.) и мягким перетаскиванием (drag-and-drop) раскидывать эффекты из общего пула по ценовым категориям.
- **Интерфейс:** Открывается на весь экран поверх приложения, имеет кнопки "Сохранить и закрыть" и "Назад" (без сохранения).
- **No-Code создание:** (В будущем) конструктор кастомных эффектов из базовых блоков.

---''', text)

with open(path, 'w', encoding='utf-8') as f:
    f.write(text)
